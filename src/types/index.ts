export interface Sala {
    id: number;
    title: string;
    description: string;
    imageUrl: string;
  }
  
  export interface Service {
    title: string;
    description: string;
    imageUrl: string;
  }
  
  export interface News {
    title: string;
    description: string;
    imageUrl: string;
  }